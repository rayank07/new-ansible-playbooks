- name: Install apt using task
  hosts: "{{ host }}"
  remote_user: ubuntu
  become: yes
  connection: ssh
  gather_facts: yes

  tasks:
    - name: Install apt package
      package:
        name: apt
        state: present
