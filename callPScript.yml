- name: Example playbook for Windows
  hosts: "{{ host }}"
  gather_facts: no
  become: none

 

  vars:
    ansible_connection: winrm
    ansible_port: ignore
    ansible_winrm_transport: ignore
    ansible_winrm_server_cert_validation: ignore

 

  tasks:
    - name: Run some task on Windows
      win_command: ipconfig
